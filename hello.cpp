#include <bits/stdc++.h>
#include "pp.hpp"
using namespace std;

int main() {
    int x;
    vector<int> input;
    while(cin >> x) {
        input.push_back(x);
    }

    cout << input << "\n";
    return 0;
}